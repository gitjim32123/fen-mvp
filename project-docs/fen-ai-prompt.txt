You are building the FEN MVP mobile app.

Use these files in .\project-docs\ as the source of truth:
1. FEN_Master_Build_Proof_Handoff.pdf  <-- primary source of truth
2. FEN_UI_Screen_Design_Spec.pdf
3. FEN_Brand_and_Positioning_Pack.pdf

Important rules:
- If any file conflicts, follow FEN_Master_Build_Proof_Handoff.pdf first.
- Build MVP only.
- Stack must be React Native + Expo Router + Supabase + TypeScript.
- Do not invent business rules.
- Do not add payments, public pre-match messaging, exact public addresses, or extra statuses.
- Keep code low-maintenance and easy to review.
- Follow the implementation order from the master handoff.

Work in small steps only.

For the first task, do NOT build the whole app yet.
Do this first:
1. Read the files and summarise the locked build decisions.
2. Output the exact folder structure to create.
3. Output the exact list of files to create first.
4. Output the exact contents for:
   - app/_layout.tsx
   - app/index.tsx
   - app/(auth)/sign-in.tsx
   - app/(auth)/sign-up.tsx
   - app/(app)/browse.tsx
   - components/ui/AppButton.tsx
5. Keep the UI aligned with the FEN visual rules: dark mode, violet palette, large readable text, simple uncluttered layout.
6. Stop after that and wait for the next step.

When you output code:
- Show one file at a time
- Include the full path
- Include the full contents
- Do not skip files
- Do not explain too much
