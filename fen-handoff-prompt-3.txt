You are continuing the FEN MVP project for Fast Earn Nearby.

Use these files as source of truth:
- .\project-docs\FEN_Master_Build_Proof_Handoff.pdf
- .\project-docs\FEN_UI_Screen_Design_Spec.pdf
- .\project-docs\FEN_Brand_and_Positioning_Pack.pdf
- .\project-docs\FEN_Browse_Map_Addendum.txt

Important product rules:
- MVP stack is React Native + Expo Router + Supabase + TypeScript
- Browsing is public-safe and postcode-based
- Browse screen must include a lightweight map at the top and a list underneath
- Estimated travel time is required on each Browse card
- Exact addresses must never be shown publicly
- Messaging only unlocks after worker selection
- After cancellation or completion, conversation is archived/read-only
- Reports and blocks are in MVP
- Reviews and payments are not in MVP
- AI helpers are optional only

Status lifecycle:
- open
- held
- confirm_pending
- in_progress
- completed
- cancelled

Allowed transitions:
- open -> held
- held -> confirm_pending
- confirm_pending -> in_progress
- in_progress -> completed
- open / held / confirm_pending -> cancelled
- cancelled -> open only if work never started

Current files I want you to inspect and update first:
- .\lib\types.ts
- .\lib\jobs.ts
- .\lib\messaging.ts
- .\app\app\my-jobs.tsx
- .\app\app\messages.tsx
- .\app\app\job\[id].tsx

Rules for your output:
1. Do not guess file names or schema fields.
2. Use the actual existing code in those files.
3. Make the smallest safe edits needed.
4. Show full file contents one file at a time.
5. Stop after the first batch and wait for the next step.
