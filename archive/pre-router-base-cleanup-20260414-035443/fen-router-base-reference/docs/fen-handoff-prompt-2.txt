You are editing an existing React Native Expo Router project for FEN - Fast Earn Nearby.

Important:
- Do NOT describe the work at a high level.
- Do NOT suggest charts, tables, dashboards, or generic architecture.
- Do NOT restart the app.
- Do NOT invent new statuses, screens, or business rules.
- Output real code changes only.

Project files that already exist and must be inspected first:
- .\lib\types.ts
- .\lib\jobs.ts
- .\lib\applications.ts
- .\lib\messaging.ts
- .\lib\supabase.ts
- .\app\app\my-jobs.tsx
- .\app\app\messages.tsx
- .\app\app\post.tsx
- .\components\jobs\StatusChip.tsx
- .\components\ui\AppButton.tsx
- .\components\ui\AppTextField.tsx

Source-of-truth product rules:
- Allowed statuses only: open, held, confirm_pending, in_progress, completed, cancelled
- Allowed transitions only:
  open -> held
  held -> confirm_pending
  confirm_pending -> in_progress
  in_progress -> completed
  open/held/confirm_pending -> cancelled
  cancelled -> open only if work never started
- Cancellation reason is required where defined by the handoff
- Conversations become archived/read-only after cancelled or completed
- Reports must support user, job, and message
- Blocks must stop normal message access in the UI
- AI helpers are optional only on post.tsx
- No reviews
- No payments
- No maps
- No exact public addresses

Task:
Implement ONLY the first batch:
1. status lifecycle support
2. cancellation/reopen logic
3. message archiving for cancelled/completed jobs

Instructions:
1. Start by listing ONLY the files you will change from the files above.
2. Then output the FULL contents of each changed file, one file at a time.
3. Use the real existing project structure and imports.
4. Keep styling consistent with the current FEN app.
5. End with a short test checklist.
6. Do not output pseudo-code.
