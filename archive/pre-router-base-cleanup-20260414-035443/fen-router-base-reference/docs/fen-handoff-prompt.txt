You are completing the FEN MVP from fen-router-base.

CURRENT STATE (all implemented):
- Router + screens (browse/post/my-jobs/messages/profile/auth)
- Supabase schema, auth, jobs, applications, messaging
- lib/supabase.ts, auth.ts, jobs.ts, applications.ts, messaging.ts, types.ts
- Components: JobCard, StatusChip, AppButton, AppTextField

Use attached FEN PDFs as source of truth.

NEXT TASKS ONLY (in order):
1. Status transitions (open->held->confirm_pending->in_progress->completed->cancelled)
2. Cancellation/reopen logic
3. Reports (user/job/message)
4. Blocks (stop chat access)
5. Optional AI helpers on post.tsx only

Rules:
- No new statuses
- Cancel needs reason
- Conversations archive on cancel/complete
- No reviews, payments, maps
- Keep violet theme, large text, simple

1. Inspect lib/types.ts, lib/jobs.ts, my-jobs.tsx, messages.tsx
2. Output changed files FULL CODE one by one
3. End with test checklist

Additional locked clarification:
Apply .\project-docs\FEN_Browse_Map_Addendum.txt together with the main FEN handoff.
Browse must not be list-only.
Browse must include:
- a lightweight map at the top
- approximate public pins only
- user location marker if permitted
- estimated travel time on every job card

If there is any conflict with earlier simplified Browse assumptions, the addendum takes precedence.
